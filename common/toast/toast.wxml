<template name="toast">
	<view class="toast" wx:if="{{ isShowToast }}">
		<view class="loading" wx:if="{{ icon == 'waiting' }}"></view>
		<icon wx:else type="{{ icon }}" size="50" color="#eee"/>
		<text wx:if="{{ content }}">{{ content }}</text>
	</view>
</template>

