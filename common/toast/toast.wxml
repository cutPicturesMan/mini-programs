<template name="toast">
	<view class="toast" wx:if="{{ content }}">
		<icon type="{{ icon }}" size="50" color="#eee"/>
		<text>{{ content }}</text>
	</view>
</template>
<import src="../../../common/toast/toast.wxml"/>

<template is="toast" data="{{ ...toast }}" />
